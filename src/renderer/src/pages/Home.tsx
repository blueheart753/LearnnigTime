import AsideBar from '@renderer/components/AsideBar';
import Watch from '@renderer/components/Watch';

const Home = () => {
  return (
    <div className="Home">
      <AsideBar />
      <Watch />
    </div>
  );
};

export default Home;
