import SideContents from './SideContents';

const AsideBar = () => {
  return (
    <aside>
      <SideContents title="대시보드" target_url="DashBoard" current_title="엄준식" />
    </aside>
  );
};

export default AsideBar;
